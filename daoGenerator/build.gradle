apply plugin: 'java'
apply plugin: 'application'

outputDir = "../app/src/main/java"
mainClassName = "com.example.mymodule.generator.Generator"

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile('de.greenrobot:DaoGenerator:1.3.0')
}

run {
    args outputDir
}